<!doctype html>
<html lang="en" dir="ltr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Sign Up — ILIYA</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg-start: #071029;
      --bg-end:   #0f172a;
      --accent1:  #06b6d4; /* teal */
      --accent2:  #8b5cf6; /* violet */
      --glass:    rgba(255,255,255,0.06);
      --glass-2:  rgba(255,255,255,0.03);
      --text:     #ffffff;
      --muted:    rgba(255,255,255,0.82);
      --danger:   #ff6b6b;
      --radius:   16px;
      --card-w:   460px;
    }

    /* base */
    html,body{height:100%}
    body{
      margin:0;
      font-family: 'Poppins', system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      color:var(--text);
      background: linear-gradient(135deg,var(--bg-start), var(--bg-end));
      display:flex;
      align-items:center;
      justify-content:center;
      padding:28px;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
    }

    /* moving color overlays */
    .bg-animate{
      position:fixed;
      inset:0;
      z-index:0;
      pointer-events:none;
      background:
        radial-gradient(600px 420px at 10% 10%, rgba(8,145,178,0.12), transparent 10%),
        radial-gradient(520px 380px at 85% 80%, rgba(139,92,246,0.10), transparent 12%);
      filter: blur(34px) saturate(1.1);
      animation: floatBG 11s ease-in-out infinite alternate;
    }
    @keyframes floatBG{
      from{transform:translateY(0) rotate(0deg) scale(1)}
      to{transform:translateY(-20px) rotate(3deg) scale(1.03)}
    }

    .wrap{position:relative; z-index:2; width:100%; max-width:var(--card-w)}

    .card{
      border-radius:var(--radius);
      padding:26px;
      background: linear-gradient(180deg, rgba(255,255,255,0.03), rgba(255,255,255,0.015));
      box-shadow: 0 18px 40px rgba(2,6,23,0.6), inset 0 1px 0 rgba(255,255,255,0.02);
      backdrop-filter: blur(10px) saturate(1.05);
      border: 1px solid rgba(255,255,255,0.04);
      overflow:hidden;
      animation: cardPop .45s cubic-bezier(.2,.9,.3,1);
    }
    @keyframes cardPop{
      from{opacity:0; transform:translateY(12px) scale(.995)}
      to{opacity:1; transform:none}
    }

    /* header */
    .head{display:flex; gap:14px; align-items:center; margin-bottom:8px}
    .logo{
      width:66px; height:66px; border-radius:14px;
      background: linear-gradient(135deg,var(--accent2), var(--accent1));
      display:grid; place-items:center;
      box-shadow: 0 10px 30px rgba(8,145,178,0.12);
      flex-shrink:0;
    }
    .logo svg{width:36px;height:36px; filter:drop-shadow(0 8px 20px rgba(0,0,0,0.35));}

    .title h1{margin:0; font-size:20px; font-weight:700}
    .title p{margin:6px 0 0; font-size:13px; color:var(--muted)}

    .badge{
      display:inline-block; margin-top:8px; font-size:12px; padding:6px 10px; border-radius:999px;
      background: linear-gradient(90deg, rgba(255,255,255,0.03), rgba(255,255,255,0.01));
      color:var(--muted); border:1px solid rgba(255,255,255,0.03);
    }

    form{display:grid; gap:12px; margin-top:6px}

    .input-group{
      display:flex; align-items:center; gap:12px;
      background: linear-gradient(180deg,var(--glass),var(--glass-2));
      border-radius:12px; padding:12px; border:1px solid transparent;
      transition: transform .15s ease, box-shadow .15s ease, border-color .15s ease;
    }
    .input-group:focus-within{
      transform: translateY(-3px);
      box-shadow: 0 18px 36px rgba(2,6,23,0.45);
      border-color: rgba(255,255,255,0.08);
    }

    .input-icon{
      width:40px; height:40px; border-radius:10px;
      display:grid; place-items:center; flex-shrink:0;
      background: linear-gradient(180deg, rgba(255,255,255,0.03), rgba(255,255,255,0.01));
    }
    .input-icon svg{width:18px;height:18px; opacity:0.95}

    input{
      background:transparent; border:0; outline:0; color:var(--text);
      font-size:15px; width:100%; padding:6px 6px 6px 6px;
    }

    /* floating label (LTR) */
    .floating{position:relative; width:100%}
    .floating label{
      position:absolute; left:12px; top:50%; transform:translateY(-50%);
      font-size:13px; color:var(--muted); pointer-events:none;
      transition: all .16s ease;
      padding:0 6px;
    }
    .floating input:focus + label,
    .floating input:not(:placeholder-shown) + label{
      top:-10px; font-size:12px; color:#fff; transform:none;
      background: linear-gradient(90deg, rgba(0,0,0,0.12), rgba(0,0,0,0.04));
      border-radius:6px;
    }

    .help{font-size:12px; color:var(--muted)}
    .error{font-size:12px; color:var(--danger); display:none}
    .input-group.invalid .error{display:block}

    .cta{
      margin-top:6px; display:block; width:100%; padding:14px;
      border-radius:12px; font-weight:700; font-size:16px; border:0; cursor:pointer;
      background: linear-gradient(90deg, var(--accent1), var(--accent2));
      box-shadow: 0 12px 34px rgba(8,145,178,0.14);
      color:white; transition: transform .14s ease, box-shadow .14s ease;
    }
    .cta:hover{ transform:translateY(-3px); box-shadow: 0 22px 48px rgba(139,92,246,0.16) }
    .cta:active{ transform:translateY(-1px) }

    .foot{display:flex; justify-content:space-between; align-items:center; margin-top:8px; gap:10px; font-size:13px; color:var(--muted)}
    .link{ text-decoration:none; color:rgba(255,255,255,0.95); font-weight:600; padding:6px 10px; border-radius:10px; background:linear-gradient(90deg, rgba(255,255,255,0.03), rgba(255,255,255,0.01)) }

    @media (max-width:520px){
      :root{--card-w:94vw}
      .logo{width:56px;height:56px}
    }

    input:focus{ outline:2px solid rgba(255,255,255,0.04); outline-offset:2px }
    /* small accessible subtle animation on logo */
    .logo{ animation: logoPulse 6s infinite ease-in-out alternate; }
    @keyframes logoPulse{ from{transform:translateY(0)} to{transform:translateY(-6px) scale(1.01)} }
  </style>
</head>
<body>
  <div class="bg-animate" aria-hidden="true"></div>

  <main class="wrap" role="main" aria-labelledby="signup-title">
    <section class="card" aria-label="Sign up form">
      <header class="head">
        <div class="logo" aria-hidden="true">
          <!-- simple SVG monogram -->
          <svg viewBox="0 0 100 100" fill="none" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Logo">
            <rect x="0" y="0" width="100" height="100" rx="16" fill="url(#g)"/>
            <defs>
              <linearGradient id="g" x1="0" x2="1">
                <stop offset="0" stop-color="#8b5cf6"/>
                <stop offset="1" stop-color="#06b6d4"/>
              </linearGradient>
            </defs>
            <g fill="#fff" opacity="0.98">
              <path d="M64 28H44a2 2 0 0 0-2 2v8H36v8h6v20h8V46h10l2-8H50V30h14a0 0 0 0 1 0 0z"/>
            </g>
          </svg>
        </div>

        <div class="title">
          <h1 id="signup-title">Welcome — Create your account</h1>
          <p class="badge">Secure • Fast • Ready to go</p>
        </div>
      </header>

      <form novalidate>
        <!-- Email -->
        <div class="input-group" aria-live="polite">
          <span class="input-icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M3 8.5v7a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path>
              <path d="M21 7l-9 6L3 7"></path>
            </svg>
          </span>
          <div class="floating">
            <input id="email" name="email" type="email" placeholder=" " required aria-required="true" autocomplete="email">
            <label for="email">Email</label>
          </div>
        </div>

        <!-- Password -->
        <div class="input-group">
          <span class="input-icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <rect x="3" y="11" width="18" height="11" rx="2"/>
              <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
            </svg>
          </span>
          <div class="floating">
            <input id="password" name="password" type="password" placeholder=" " required aria-required="true" autocomplete="new-password" minlength="6">
            <label for="password">Password</label>
          </div>
        </div>

        <!-- Confirm Password -->
        <div class="input-group">
          <span class="input-icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M21 12a9 9 0 1 0-3.2 6.6L21 18"></path>
              <path d="M21 7v5h-5"></path>
            </svg>
          </span>
          <div class="floating">
            <input id="confirm" name="confirm" type="password" placeholder=" " required aria-required="true" autocomplete="new-password" minlength="6">
            <label for="confirm">Confirm Password</label>
          </div>
        </div>

        <!-- Phone -->
        <div class="input-group">
          <span class="input-icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M22 16.92V21a1 1 0 0 1-1.11 1 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6A19.79 19.79 0 0 1 2 3.11 1 1 0 0 1 3 2h4.09a1 1 0 0 1 1 .75c.12.6.36 1.18.7 1.72a1 1 0 0 1-.24 1.1L8.2 7.8a16 16 0 0 0 6 6l1.2-1.2a1 1 0 0 1 1.1-.24c.54.34 1.12.58 1.72.7a1 1 0 0 1 .75 1V21z"></path>
            </svg>
          </span>
          <div class="floating">
            <input id="phone" name="phone" type="tel" placeholder=" " required aria-required="true" autocomplete="tel" pattern="^\+?\d{8,15}$">
            <label for="phone">Phone (e.g. +1 555 ...)</label>
          </div>
        </div>

        <button type="submit" class="cta" aria-label="Create account">Create Account — Get Started</button>

        <div class="foot">
          <span class="help">By creating an account you agree to our Terms.</span>
          <a href="#" class="link" aria-label="Sign in">Already have an account?</a>
        </div>
      </form>
    </section>
  </main>
</body>
</html>
